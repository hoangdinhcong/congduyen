# Environment variables template
# Copy this file to .env.local and fill in your actual values

# Deployment target - options: development, vercel, netlify
DEPLOY_TARGET=development

# Base URL configuration for metadata and OpenGraph
# This should be your actual domain in production
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Deployment platform detection (these will be set automatically on the platforms)
# VERCEL=1           # Set automatically on Vercel
# NETLIFY=true       # Set automatically on Netlify

# Supabase Configuration
# Replace these with your actual Supabase project credentials
NEXT_PUBLIC_SUPABASE_URL=your-supabase-project-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key

# Admin Authentication
# Generate this using bcrypt - example: await bcrypt.hash('your-secure-password', 10)
ADMIN_PASSWORD_HASH=your-bcrypt-hashed-password

# Optional: Custom domains for your deployments
# NEXT_PUBLIC_VERCEL_URL=congduyen.vercel.app
# NEXT_PUBLIC_NETLIFY_URL=congduyen.netlify.app
